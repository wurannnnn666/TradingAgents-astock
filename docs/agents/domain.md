# Domain Docs

This repo uses a single-context domain documentation layout.

Skills that need domain context should read:

- `CONTEXT.md` at the repository root, when present.
- ADRs under `docs/adr/`, when present.

If the project later becomes a monorepo or splits into multiple product contexts, replace this file with a multi-context layout and add `CONTEXT-MAP.md`.

